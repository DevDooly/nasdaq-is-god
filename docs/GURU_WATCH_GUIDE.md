# 📡 Guru Watch 실시간 연동 및 자동 매매 가이드

이 문서는 시장 영향력 있는 인물의 소셜 미디어(X/Twitter) 발언을 실시간으로 수집하고, AI 분석을 거쳐 즉각적인 자동 매매로 연결하는 시스템의 설정 및 사용 방법을 설명합니다.

---

## 1. 시스템 워크플로우 (Workflow)

전체 프로세스는 다음과 같은 **'Zero-Latency'** 파이프라인으로 작동합니다.

1.  **감지 (Detection)**: 외부 자동화 도구(Pipedream 등)가 설정된 인물의 새로운 트윗을 감지합니다.
2.  **전송 (Ingestion)**: 감지된 데이터를 우리 서버의 보안 Webhook 엔드포인트(`POST /webhook/guru-alpha`)로 전송합니다.
3.  **분석 (AI Analysis)**: Gemini 2.0 Flash 모델이 해당 발언의 시장 파급력을 0~100점으로 즉시 수치화합니다.
4.  **판단 및 실행 (Execution)**: 
    - 점수가 **90점 이상**이며 해당 인물의 자동매매가 활성화된 경우: **즉시 매수(BUY)**
    - 점수가 **10점 이하**이며 해당 인물의 자동매매가 활성화된 경우: **즉시 매도(SELL)**
5.  **기록 및 알림 (Reporting)**: 모든 과정은 DB에 저장되며 텔레그램 및 웹 대시보드로 실시간 푸시 알림이 발송됩니다.

---

## 2. 서버 설정 (Server Setup)

### A. 환경 변수 설정
`.env` 파일에 Webhook 인증을 위한 비밀키를 설정해야 합니다.
```bash
WEBHOOK_SECRET=your_custom_secret_key_here
```

### B. 구루 권한 설정
모든 인물의 발언에 대해 무조건 매매가 나가는 것은 위험합니다. 특정 인물에게만 매매 권한을 줄 수 있습니다.
1. 웹 대시보드 접속 -> **GURU ALPHA** 메뉴 이동.
2. **DIRECTORY & IMPACT** 탭에서 대상 인물을 확인합니다.
3. (추후 업데이트 예정) 현재는 DB에서 `is_auto_trade_enabled` 값을 `TRUE`로 변경한 인물에 대해서만 자동 매매가 실행됩니다.

---

## 3. 외부 브릿지 연동 (Pipedream 상세 가이드)

트위터(X)의 새 글을 실시간으로 감지하여 우리 서버로 전달해주는 **Pipedream** 설정 방법입니다.

### Step 1: Pipedream 워크플로우 생성
1. [Pipedream](https://pipedream.com/)에 로그인합니다.
2. 상단 **[New Workflow]** 버튼을 클릭합니다.

### Step 2: Trigger (Twitter/X) 설정
1. 검색창에 `X` 또는 `Twitter`를 입력하고 **[New Tweet from User]**를 선택합니다.
2. **X Account**: 자신의 X 계정을 연동합니다 (OAuth 인증).
3. **Screen Name**: 추적할 인물의 아이디를 입력합니다 (예: `elonmusk`). 
   *   *팁: 여러 명을 추적하려면 워크플로우를 각각 만들거나, `New Tweet from Search` 트리거를 사용하여 `from:elonmusk OR from:realDonaldTrump` 쿼리를 사용하세요.*
4. **Ignore Retweets**: `True`로 설정하여 본인의 순수 발언만 필터링하는 것을 권장합니다.
5. **[Create Source]**를 클릭하여 트리거를 활성화합니다.

### Step 3: Action (HTTP Request) 추가
1. 트리거 아래의 **[+]** 아이콘을 클릭하여 새 단계를 추가합니다.
2. `HTTP`를 검색하여 **[Send Any HTTP Request]**를 선택합니다.
3. 다음과 같이 세부 정보를 입력합니다:
   - **Method**: `POST`
   - **URL**: `http://your-server-ip:9000/webhook/guru-alpha` (사장님의 서버 공인 IP 또는 도메인)
   - **Headers**:
     - `X-Alpha-Secret`: `.env`에 설정한 `WEBHOOK_SECRET` 값을 입력합니다. (예: `your_super_secret_key`)
     - `Content-Type`: `application/json`
   - **Body (JSON)**: 우측의 아이콘을 눌러 `{{ }}` 형식의 변수를 삽입하거나 아래 코드를 복사해서 붙여넣으세요.
     ```json
     {
       "handle": "@{{steps.trigger.event.user.screen_name}}",
       "text": "{{steps.trigger.event.full_text}}",
       "url": "https://twitter.com/i/web/status/{{steps.trigger.event.id_str}}"
     }
     ```

### Step 4: 테스트 및 배포 (Deploy)
1. **[Test]** 버튼을 눌러 실제 데이터가 사장님의 서버로 전송되는지 확인합니다.
2. 서버 로그(`api_server.log`)에 `⚡ [REAL-TIME] Analyzing post...` 메시지가 뜨면 성공입니다.
3. 우측 상단의 **[Deploy]**를 눌러 워크플로우를 실시간 가동합니다.

---

## 4. 매매 로직 상세 (Trading Logic)

시스템은 **'Strong Signal'**에만 반응하도록 설계되었습니다.

- **매수(BUY) 조건**: 
    - AI 분석 결과 `score >= 90`
    - `is_auto_trade_enabled == True` (해당 인물)
    - `is_auto_trading_enabled == True` (마스터 스위치)
- **매도(SELL) 조건**:
    - AI 분석 결과 `score <= 10`
    - 자산 보유 중인 경우 실행
- **기본 수량**: 현재 설정값은 **1.0주**입니다. (`main_api.py`에서 수정 가능)

---

## 5. 모니터링 및 분석

수집된 데이터와 실행 결과는 다음 페이지에서 확인 가능합니다.

1.  **WATCH TIMELINE**: 실시간으로 분석 중인 트윗과 점수 확인.
2.  **HISTORICAL ARCHIVE**: 날짜별 발언 아카이브 및 **발언 당시의 주가** 기록 확인.
3.  **Telegram**: 매매 체결 여부 및 AI 분석 요약 보고서 수신.

---

## ⚠️ 주의 사항 및 리스크 관리

- **API 쿼터**: Gemini 무료 티어 사용 시 연달아 트윗이 올라오면 분석이 실패할 수 있습니다. (유료 전환 시 해결 가능)
- **오보 대응**: 해킹된 계정의 발언에 대한 방어 로직은 현재 개발 중입니다. 극단적인 변동성 발생 시 마스터 스위치로 자동 매매를 일시 중단하십시오.
- **슬리피지**: 발언 후 주문까지의 레이턴시가 짧을수록 유리하지만, 시장가 주문 시 슬리피지가 발생할 수 있습니다.

---
**최종 업데이트**: 2026-02-14
**작성자**: Senior Quant Developer (Nasdaq is God)
